#!/bin/sh

herd spawn transient --service-name=cam \
                     --environment-variable="WAYLAND_DISPLAY=$WAYLAND_DISPLAY" \
                     -- mpv av://v4l2:/dev/video0 --profile=low-latency \
                                                  --vf=hflip \
                                                  --untimed
