#+title: Pip management from Guile

** TODO improve Pip package management

#+begin_src scheme

  (display "\n>>= installing pip packages\n")

  (for-each
   (lambda(x) (pip-install x))
   '("pyright" "flake8" ))

#+end_src
